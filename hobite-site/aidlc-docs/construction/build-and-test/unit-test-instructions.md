# NVIDIA Research Hub Unit Test Instructions

- The project has no dedicated research-page unit suite.
- The SEC generator validates source response status and fails on unavailable endpoints.
- TypeScript production builds validate typed datasets, imports and render contracts.
